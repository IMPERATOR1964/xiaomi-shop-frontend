const fs = require('fs');
const { Document, Packer, Paragraph, TextRun, AlignmentType } = require('docx');
const OUT = 'C:/Users/safik/Desktop/Сафиканов доклад к защите.docx';
const PT = (p) => p * 2;
const L = 360, FL = 709;

const title = (t) => new Paragraph({ alignment: AlignmentType.CENTER, spacing: { after: 120, line: L }, children: [new TextRun({ text: t, bold: true, size: PT(15) })] });
const sub = (t) => new Paragraph({ alignment: AlignmentType.CENTER, spacing: { after: 240, line: L }, children: [new TextRun({ text: t, italics: true, size: PT(12) })] });
const head = (t) => new Paragraph({ spacing: { before: 180, after: 40, line: L }, keepNext: true, children: [new TextRun({ text: t, bold: true })] });
const body = (t) => new Paragraph({ alignment: AlignmentType.JUSTIFIED, spacing: { after: 0, line: L }, indent: { firstLine: FL }, children: [new TextRun({ text: t })] });
const note = (t) => new Paragraph({ alignment: AlignmentType.JUSTIFIED, spacing: { after: 0, line: L }, indent: { firstLine: FL }, children: [new TextRun({ text: t, italics: true })] });

const c = [];
c.push(title('Текст доклада к защите дипломного проекта'));
c.push(sub('«Voltix» — разработка клиентской части веб-сайта для продажи смартфонов Xiaomi и аксессуаров'));

c.push(head('Слайд 1 — Титульный'));
c.push(body('Уважаемые члены комиссии! Тема моего дипломного проекта — «Разработка клиентской части веб-сайта «Voltix» для продажи смартфонов Xiaomi и аксессуаров». Это одностраничное веб-приложение интернет-магазина, выполненное на React и Vite.'));

c.push(head('Слайд 2 — Фронтенд как драйвер продаж. Цель'));
c.push(body('Сегодня всё больше покупателей выбирают технику онлайн, и удобство сайта напрямую влияет на продажи: медленный или неудобный интерфейс ведёт к потере клиентов. Поэтому цель проекта — разработать удобный, адаптивный и быстрый фронтенд для покупки техники Xiaomi. Разрабатывалась именно клиентская часть; серверную часть я не создавал — фронтенд получает от неё данные по REST API.'));

c.push(head('Слайд 3 — Инструментарий и архитектура'));
c.push(body('Основа — язык JavaScript. Интерфейс построен на библиотеке React, навигация без перезагрузки страниц — на React Router, сборка проекта — на Vite, а обмен данными с сервером идёт по REST API через fetch. Такой набор обеспечивает высокую скорость работы и удобство сопровождения.'));

c.push(head('Слайд 4 — Состояние и JWT-авторизация'));
c.push(body('Общее состояние приложения — корзина, авторизация, тема, избранное, сравнение — вынесено в контексты React, поэтому к ним можно обращаться из любого компонента. Авторизация построена на маркерах JWT: после входа токен сохраняется и добавляется в заголовок запросов, а доступ к закрытым разделам разграничивается по ролям.'));

c.push(head('Слайд 5 — Алгоритм работы (путь пользователя)'));
c.push(body('Здесь показан путь пользователя: запуск приложения, просмотр каталога с фильтрами и поиском, открытие карточки товара, добавление в корзину. Оформление заказа доступно только авторизованному пользователю — если он не вошёл, приложение перенаправляет его на вход, а потом возвращает к оформлению. Данные заказа уходят на сервер по REST API.'));

c.push(head('Слайд 6 — Дизайн-система и функционал'));
c.push(body('Интерфейс выполнен в современном стиле с оранжевым акцентом, адаптивной вёрсткой под ПК, планшет и телефон, со светлой и тёмной темами. Реализован полный функционал магазина: каталог с фильтрами и сортировкой, выбор объёма памяти и цвета, избранное и сравнение, отзывы с оценкой и средним рейтингом, рекомендации.'));

c.push(head('Слайд 7 — Роли и панель администратора'));
c.push(body('Для сотрудников реализована панель администратора с разграничением прав по ролям — администратор и модератор. Доступ защищён: маршруты открываются только при наличии нужной роли в токене. В панели — управление товарами и категориями, загрузка фотографий и работа с заказами: просмотр и изменение их статусов.'));

c.push(head('Слайд 8 — Демонстрация (Live-демо)'));
c.push(body('Предлагаю увидеть приложение в действии.'));
c.push(note('Показать за ~1 минуту: главная страница → каталог с фильтром → карточка товара (выбор памяти и цвета, добавление в корзину) → корзина → вход в панель администратора. Параллельно показать переключение тёмной темы и сужение окна браузера (адаптивность).'));

c.push(head('Слайд 9 — Итоги и развитие'));
c.push(body('Подведу итоги. Разработана клиентская часть интернет-магазина «Voltix», готовая к интеграции с серверной частью. Реализован адаптивный SPA-интерфейс, настроено взаимодействие по REST API с авторизацией JWT, реализована логика корзины, фильтров и ролевой модели. В дальнейшем возможно подключение онлайн-оплаты, интеграция со службами доставки и уведомления о статусах заказов. Спасибо за внимание, готов ответить на ваши вопросы!'));

c.push(new Paragraph({ spacing: { before: 220, line: L }, children: [new TextRun({ text: 'Тайминг: ~25–30 секунд на слайды 1–7, ~1 минута на демонстрацию (слайд 8), ~25 секунд на итоги. Всего около 5 минут. Не зачитывайте — рассказывайте своими словами.', italics: true, size: PT(12) })] }));

const doc = new Document({
  styles: { default: { document: { run: { font: 'Times New Roman', size: PT(14) } } } },
  sections: [{ properties: { page: { size: { width: 11906, height: 16838 }, margin: { top: 1134, right: 850, bottom: 1134, left: 1418 } } }, children: c }],
});
Packer.toBuffer(doc).then(b => { fs.writeFileSync(OUT, b); console.log('Written:', OUT, b.length); });
