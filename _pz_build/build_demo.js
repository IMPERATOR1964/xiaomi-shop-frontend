const fs = require('fs');
const { Document, Packer, Paragraph, TextRun, AlignmentType } = require('docx');
const OUT = 'C:/Users/safik/Desktop/Сафиканов шпаргалка показ сайта.docx';
const PT = (p) => p * 2;
const L = 360, FL = 0;

const title = (t) => new Paragraph({ alignment: AlignmentType.CENTER, spacing: { after: 80, line: L }, children: [new TextRun({ text: t, bold: true, size: PT(15) })] });
const sub = (t) => new Paragraph({ alignment: AlignmentType.CENTER, spacing: { after: 220, line: L }, children: [new TextRun({ text: t, italics: true, size: PT(11) })] });
const head = (t) => new Paragraph({ spacing: { before: 170, after: 30, line: L }, keepNext: true, children: [new TextRun({ text: t, bold: true, size: PT(14) })] });
const showLine = (t) => new Paragraph({ spacing: { after: 0, line: L }, children: [new TextRun({ text: 'Показываю: ', bold: true }), new TextRun({ text: t })] });
const implLine = (t) => new Paragraph({ spacing: { after: 0, line: L }, children: [new TextRun({ text: 'Реализация (фронт): ', bold: true, italics: true }), new TextRun({ text: t, italics: true })] });
const sayLine = (t) => new Paragraph({ spacing: { after: 0, line: L }, children: [new TextRun({ text: 'Фраза: ', bold: true }), new TextRun({ text: '«' + t + '»' })] });

const c = [];
c.push(title('Шпаргалка для показа сайта «Voltix» (фронтенд-часть)'));
c.push(sub('Что нажимать и как кратко объяснить реализацию каждой страницы'));

const pages = [
  ['1. Главная страница',
    'хедер с логотипом, поиском и иконками; блок категорий; подборки товаров; блок «Вы недавно смотрели».',
    'отдельный React-компонент HomePage. Товары и категории загружаются с сервера через слой API (productsApi). Рекомендации формируются по истории просмотров и избранному, недавно просмотренные берутся из контекста истории.',
    'Это главная страница. Она реализована как React-компонент: данные о товарах подгружаются с сервера по REST API, а рекомендации и недавно просмотренные собираются из состояния приложения.'],
  ['2. Каталог',
    'слева — панель фильтров по характеристикам, цене и наличию; сверху — сортировка; список карточек товаров. Меняю фильтр — список обновляется без перезагрузки.',
    'компонент CatalogPage + компонент фильтров CatalogFilter. Список грузится одним запросом, фильтрация и сортировка идут на клиенте через состояние React, поэтому страница не перезагружается. Опции фильтров строятся из полного набора товаров категории.',
    'Каталог: фильтры и сортировка работают мгновенно на стороне клиента, без перезагрузки страницы — за счёт состояния React.'],
  ['3. Карточка товара',
    'характеристики, выбор объёма памяти и цвета, кнопки «в корзину», «в избранное», «к сравнению», отзывы и блок похожих товаров.',
    'компонент ProductPage. Выбор памяти/цвета реализован компонентом ProductVariants — он переключает на нужный вариант товара. Добавление в корзину/избранное/сравнение идёт через контексты. Отзывы и похожие товары — отдельные компоненты, данные с сервера. Просмотр запоминается в истории.',
    'Карточка товара: выбор варианта по памяти и цвету, добавление в корзину, избранное и сравнение — всё через общее состояние приложения (контексты React).'],
  ['4. Корзина',
    'добавленные товары, изменение количества, удаление, итоговая сумма, кнопка оформления.',
    'компонент CartPage + контекст корзины. Для гостя корзина хранится в браузере (localStorage), для авторизованного пользователя — на сервере. При входе гостевая корзина переносится на сервер.',
    'Корзина работает и без входа: у гостя она хранится в браузере, а после авторизации переносится на сервер.'],
  ['5. Оформление заказа',
    'форма адреса и телефона, состав заказа, подтверждение. Без входа — переброс на страницу авторизации.',
    'компонент CheckoutPage. Доступен только авторизованному пользователю (проверка через контекст авторизации). Поля проверяются перед отправкой, заказ уходит на сервер по REST API, кнопка блокируется на время отправки (защита от дублей), последний адрес сохраняется.',
    'Оформление доступно только после входа; перед отправкой данные проверяются, а кнопка блокируется, чтобы заказ не задвоился.'],
  ['6. Регистрация и авторизация',
    'вход по логину и паролю, регистрация с подтверждением почты, восстановление пароля, понятные сообщения об ошибках.',
    'компоненты LoginPage/RegisterPage и контекст авторизации. После входа сервер возвращает маркер JWT, он сохраняется на клиенте и добавляется к запросам. По ролям из токена открываются закрытые разделы.',
    'Авторизация на JWT: после входа токен сохраняется и подставляется в запросы, а по ролям из него разграничивается доступ.'],
  ['7. Личный кабинет',
    'данные профиля, история заказов и их статусы, выход; для сотрудника — ссылка на админ-панель.',
    'компонент ProfilePage. Доступен только авторизованному пользователю; заказы подгружаются с сервера. Ссылка на админку показывается, только если в токене есть роль сотрудника.',
    'Личный кабинет: история заказов со статусами, а кнопка админ-панели появляется только у пользователей с нужной ролью.'],
  ['8. Избранное и сравнение',
    'добавление/удаление товаров, таблица сравнения характеристик.',
    'контексты избранного и сравнения. Содержимое сохраняется между сеансами (в браузере у гостя, на сервере у авторизованного). В сравнении из всех товаров собирается общий набор характеристик для таблицы.',
    'Избранное и сравнение сохраняются между сеансами; таблица сравнения строится из общего набора характеристик выбранных товаров.'],
  ['9. Темы и адаптивность',
    'переключаю светлую/тёмную тему; сужаю окно браузера — интерфейс перестраивается под планшет и телефон.',
    'тема хранится в контексте и применяется через переключение оформления (CSS-переменные), выбор сохраняется в браузере. Адаптивность — за счёт адаптивной вёрстки (медиа-запросы CSS).',
    'Тема переключается мгновенно и запоминается, а вёрстка адаптивная — корректно работает на ПК, планшете и телефоне.'],
  ['10. Панель администратора',
    'управление товарами и категориями, загрузка фотографий, просмотр и смена статусов заказов.',
    'раздел админки с защитой маршрутов (компонент-страж проверяет роль). Поиск товаров с задержкой (дебаунс), форма товара с загрузкой фотографий, отдельные страницы для категорий и заказов.',
    'Панель администратора защищена по роли: доступ только у администратора и модератора. Здесь — управление товарами, категориями и заказами с загрузкой фото.'],
];

pages.forEach(pg => {
  c.push(head(pg[0]));
  c.push(showLine(pg[1]));
  c.push(implLine(pg[2]));
  c.push(sayLine(pg[3]));
});

c.push(new Paragraph({ spacing: { before: 220, line: L }, children: [new TextRun({ text: 'Главное про фронт (если спросят одной фразой): это одностраничное приложение (SPA) на React и Vite; своих данных не хранит — берёт их у сервера по REST API; навигация без перезагрузки через React Router; общее состояние (корзина, авторизация, тема, избранное) — в контекстах React; авторизация на JWT.', italics: true, size: PT(12) })] }));

const doc = new Document({
  styles: { default: { document: { run: { font: 'Times New Roman', size: PT(13) } } } },
  sections: [{ properties: { page: { size: { width: 11906, height: 16838 }, margin: { top: 1020, right: 850, bottom: 1020, left: 1134 } } }, children: c }],
});
Packer.toBuffer(doc).then(b => { fs.writeFileSync(OUT, b); console.log('Written:', OUT, b.length); });
